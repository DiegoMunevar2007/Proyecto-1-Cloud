meta {
  name: CatalogAuthor
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/courses?author={{professorId}}
  auth: none
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.total).to.be.at.least(2);
}
