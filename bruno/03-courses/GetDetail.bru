meta {
  name: GetDetail
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/courses/{{courseId}}
  auth: none
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.course.status).to.equal("published");
}
