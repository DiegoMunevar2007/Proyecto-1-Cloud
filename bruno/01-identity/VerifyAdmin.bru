meta {
  name: VerifyAdmin
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/auth/verify?username={{adminUser}}&code={{adminCode}}
  auth: none
}

tests {
  // 200 fresco, 400 re-run (usuario ya verificado / código reutilizado)
  expect(res.status).to.be.oneOf([200, 400]);
}
