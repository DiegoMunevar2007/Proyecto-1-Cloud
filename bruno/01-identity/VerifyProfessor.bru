meta {
  name: VerifyProfessor
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/auth/verify?username={{professorUser}}&code={{professorCode}}
  auth: none
}

tests {
  // 200 fresco, 400 re-run (usuario ya verificado / código reutilizado)
  expect(res.status).to.be.oneOf([200, 400]);
}
