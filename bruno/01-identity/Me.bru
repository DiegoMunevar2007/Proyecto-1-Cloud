meta {
  name: Me
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.username).to.equal(bru.getEnvVar("studentUser"));
}
