meta {
  name: ReenrollB
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/enrollments/{{courseBId}}/reenroll
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(200);
}
