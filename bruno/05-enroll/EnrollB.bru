meta {
  name: EnrollB
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/enrollments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

body:json {
  {
    "course_id": {{courseBId}}
  }
}

tests {
  expect(res.status).to.be.oneOf([200, 201]);
}
