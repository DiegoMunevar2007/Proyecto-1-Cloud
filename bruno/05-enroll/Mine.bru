meta {
  name: Mine
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/enrollments
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.total).to.be.at.least(2);
}
