meta {
  name: OpenAPI
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/openapi.json
  auth: none
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.paths["/api/v1/badges"]).to.exist;
}
