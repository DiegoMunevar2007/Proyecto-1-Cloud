meta {
  name: InvalidRole
  type: http
  seq: 11
}

put {
  url: {{baseUrl}}/api/v1/admin/users/{{userId}}/role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "role": "superadmin"
  }
}

tests {
  expect(res.status).to.equal(400);
}
