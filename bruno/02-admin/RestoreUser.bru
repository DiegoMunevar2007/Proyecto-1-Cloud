meta {
  name: RestoreUser
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/admin/users/{{userId}}/restore
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  expect(res.status).to.equal(200);
}
