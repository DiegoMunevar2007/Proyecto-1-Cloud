meta {
  name: ForbiddenAsProfessor
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/users
  auth: bearer
}

auth:bearer {
  token: {{professorToken}}
}

tests {
  expect(res.status).to.equal(403);
}
