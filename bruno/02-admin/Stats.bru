meta {
  name: Stats
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/admin/stats
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.total).to.be.above(0);
}
