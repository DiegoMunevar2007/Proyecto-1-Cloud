meta {
  name: DeleteStudent2
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/api/v1/admin/users/{{userId}}
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  expect(res.status).to.equal(200);
}
