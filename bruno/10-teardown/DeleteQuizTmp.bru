meta {
  name: DeleteQuizTmp
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/api/v1/quizzes/{{tmpQuizId}}
  auth: bearer
}

auth:bearer {
  token: {{professorToken}}
}

tests {
  expect(res.status).to.equal(200);
}
