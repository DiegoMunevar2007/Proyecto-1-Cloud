meta {
  name: DeleteResourceTmp
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/resources/{{tmpResourceId}}
  auth: bearer
}

auth:bearer {
  token: {{professorToken}}
}

tests {
  expect(res.status).to.equal(200);
}
