meta {
  name: DraftForbidden
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/courses/{{courseCId}}
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(403);
}
