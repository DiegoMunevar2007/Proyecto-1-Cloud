meta {
  name: DeleteQuestionTmp
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/api/v1/quizzes/{{tmpQuizId}}/questions/{{tmpQuestionId}}
  auth: bearer
}

auth:bearer {
  token: {{professorToken}}
}

tests {
  expect(res.status).to.equal(200);
}
