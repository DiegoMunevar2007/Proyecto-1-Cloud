meta {
  name: WithdrawA
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/enrollments/{{courseId}}
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(200);
}
