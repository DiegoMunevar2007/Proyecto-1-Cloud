meta {
  name: EnrollDraft
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/enrollments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

body:json {
  {
    "course_id": {{courseCId}}
  }
}

tests {
  expect(res.status).to.equal(403);
}
