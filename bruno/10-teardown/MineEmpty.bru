meta {
  name: MineEmpty
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/api/v1/enrollments
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.total).to.equal(0);
}
